pre-commit:
  parallel: true
  commands:
    eslint:
      glob: '*.{js,jsx,ts,tsx}'
      run: npx eslint {staged_files}
    prettier:
      glob: '*.{js,jsx,ts,tsx,json,css,md}'
      run: yarn prettier {staged_files}
